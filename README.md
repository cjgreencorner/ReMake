A repo with 2 parts, it has a basic website with a function to login and register in a database.
Also a python toolscript used for multiple uses on the Raspberry Pi.
Go to 'toolscript/main.py' if you are interested in the toolscript.
